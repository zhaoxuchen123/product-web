# RK3568 · DesignWare GMAC

<script setup>
import BoardTabs from '../../../../.vitepress/theme/components/BoardTabs.vue'

const boards = [
  {
    name: 'Rockchip EVB1',
    vendor: 'Rockchip 官方评估板',
    status: 'stable',
    diffs: [
      { label: 'PHY 接口',   value: 'rgmii',                   code: true  },
      { label: '时钟方向',   value: 'output（SoC 驱动）',      code: false },
      { label: 'GMAC0 delay', value: 'tx=0x3c / rx=0x2f',     code: true  },
      { label: 'GMAC1 delay', value: 'tx=0x4f / rx=0x26',     code: true  },
      { label: 'PHY 复位',   value: 'gpio2 PD3 / gpio2 PD1',  code: true  },
    ],
    notes: [
      'clock_in_out = output，时钟由 SoC 内部 PLL（CLK_MAC_2TOP）驱动，125 MHz',
      'PHY 复位时序：延迟 0 / 20ms / 100ms',
    ],
    snippet:
`&gmac0 {
    phy-mode = "rgmii";
    clock_in_out = "output";
    tx_delay = <0x3c>;
    rx_delay = <0x2f>;
    snps,reset-gpio = <&gpio2 RK_PD3 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
    assigned-clock-parents = <...SCLK_GMAC0_RGMII_SPEED>, <...CLK_MAC0_2TOP>;
    assigned-clock-rates = <0>, <125000000>;
};
&gmac1 {
    phy-mode = "rgmii";
    clock_in_out = "output";
    tx_delay = <0x4f>;
    rx_delay = <0x26>;
    snps,reset-gpio = <&gpio2 RK_PD1 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
    assigned-clock-parents = <...SCLK_GMAC1_RGMII_SPEED>, <...CLK_MAC1_2TOP>;
    assigned-clock-rates = <0>, <125000000>;
};`,
  },
  {
    name: '创龙 TL3568-EVM',
    vendor: '创龙科技',
    status: 'stable',
    diffs: [
      { label: 'PHY 接口',    value: 'rgmii',                  code: true  },
      { label: '时钟方向',    value: 'output（继承 EVB1）',    code: false },
      { label: 'GMAC0 delay', value: 'tx=0x3c / rx=0x2f',     code: true  },
      { label: 'GMAC1 delay', value: 'tx=0x4f / rx=0x26',     code: true  },
      { label: 'PHY 复位',    value: 'gpio2 PD3 / gpio2 PD1', code: true  },
    ],
    notes: [
      'tl3568-evm.dts 完全继承 EVB1 GMAC 配置，无板级覆盖',
    ],
    snippet:
`/* 与 EVB1 配置相同，无需额外覆盖 */
/* 参考 EVB1 设备树配置即可 */`,
  },
  {
    name: 'Firefly AIO-3568J',
    vendor: 'Firefly',
    status: 'stable',
    diffs: [
      { label: 'PHY 接口',    value: 'rgmii',                  code: true  },
      { label: '时钟方向',    value: 'input（外部时钟）',      code: false },
      { label: 'GMAC0 delay', value: 'tx=0x3c / rx=0x2f',     code: true  },
      { label: 'GMAC1 delay', value: 'tx=0x4f / rx=0x26',     code: true  },
      { label: 'PHY 复位',    value: 'gpio2 PD3 / gpio2 PD1', code: true  },
    ],
    notes: [
      'clock_in_out = input，时钟由外部 PHY 提供，assigned-clock-parents 指向 gmac_clkin',
      'GMAC1 额外输出 CLK_MAC1_OUT（25 MHz）供 PHY 使用',
    ],
    snippet:
`&gmac0 {
    phy-mode = "rgmii";
    clock_in_out = "input";
    tx_delay = <0x3c>;
    rx_delay = <0x2f>;
    snps,reset-gpio = <&gpio2 RK_PD3 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
    assigned-clock-parents = <...SCLK_GMAC0_RGMII_SPEED>, <&gmac0_clkin>;
};
&gmac1 {
    phy-mode = "rgmii";
    clock_in_out = "input";
    tx_delay = <0x3a>;
    rx_delay = <0x29>;
    snps,reset-gpio = <&gpio2 RK_PD1 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
    assigned-clock-parents = <...SCLK_GMAC1_RGMII_SPEED>, <&gmac1_clkin>, <...CLK_MAC1_2TOP>;
    assigned-clock-rates = <0>, <125000000>, <25000000>;
};`,
  },
  {
    name: 'DR4（瑞朴智联）',
    vendor: '瑞朴智联',
    status: 'stable',
    diffs: [
      { label: '网口',         value: '仅 GMAC1',              code: false },
      { label: 'PHY 接口',    value: 'rgmii',                  code: true  },
      { label: '时钟方向',    value: 'input（外部时钟）',      code: false },
      { label: 'GMAC1 delay', value: 'tx=0x3a / rx=0x29',     code: true  },
      { label: 'PHY 复位',    value: 'gpio2 PD1',              code: true  },
    ],
    notes: [
      'GMAC0 未启用，仅使用 GMAC1（eth0）',
      'GMAC1 额外输出 CLK_MAC1_OUT（25 MHz）供 PHY 参考时钟',
    ],
    snippet:
`&gmac1 {
    phy-mode = "rgmii";
    clock_in_out = "input";
    tx_delay = <0x3a>;
    rx_delay = <0x29>;
    snps,reset-gpio = <&gpio2 RK_PD1 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
    assigned-clock-parents = <...SCLK_GMAC1_RGMII_SPEED>, <&gmac1_clkin>, <...CLK_MAC1_2TOP>;
    assigned-clock-rates = <0>, <125000000>, <25000000>;
};`,
  },
  {
    name: 'iTOP-3568（迅为）',
    vendor: '深圳迅为电子',
    status: 'stable',
    diffs: [
      { label: 'PHY 接口',    value: 'rgmii',                  code: true  },
      { label: '时钟方向',    value: 'input（外部时钟）',      code: false },
      { label: 'GMAC0 delay', value: 'tx=0x3c / rx=0x2f',     code: true  },
      { label: 'GMAC1 delay', value: 'tx=0x4f / rx=0x26',     code: true  },
      { label: 'PHY 复位',    value: 'gpio3 PB7 / gpio3 PB0', code: true  },
    ],
    notes: [
      'PHY 复位 GPIO 与 EVB1 不同，使用 gpio3 PB7（GMAC0）和 gpio3 PB0（GMAC1）',
    ],
    snippet:
`&gmac0 {
    phy-mode = "rgmii";
    clock_in_out = "input";
    tx_delay = <0x3c>;
    rx_delay = <0x2f>;
    snps,reset-gpio = <&gpio3 RK_PB7 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
};
&gmac1 {
    phy-mode = "rgmii";
    clock_in_out = "input";
    tx_delay = <0x4f>;
    rx_delay = <0x26>;
    snps,reset-gpio = <&gpio3 RK_PB0 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
};`,
  },
  {
    name: 'LubanCat-2（野火）',
    vendor: '野火电子',
    status: 'stable',
    diffs: [
      { label: 'PHY 接口',    value: 'rgmii',                  code: true  },
      { label: '时钟方向',    value: 'input（外部时钟）',      code: false },
      { label: 'GMAC0 delay', value: 'tx=0x2a / rx=0x19',     code: true  },
      { label: 'GMAC1 delay', value: 'tx=0x4b / rx=0x20',     code: true  },
      { label: 'PHY 复位',    value: 'gpio2 PC1 / gpio4 PB3', code: true  },
    ],
    notes: [
      'TX/RX delay 与 EVB1 不同，PCB 走线更短，delay 值更小',
      'PHY 复位 GPIO 与 EVB1 不同，注意区分',
    ],
    snippet:
`&gmac0 {
    tx_delay = <0x2a>;
    rx_delay = <0x19>;
    snps,reset-gpio = <&gpio2 RK_PC1 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <20000 20000 100000>;
};
&gmac1 {
    tx_delay = <0x4b>;
    rx_delay = <0x20>;
    snps,reset-gpio = <&gpio4 RK_PB3 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <20000 20000 100000>;
};`,
  },
  {
    name: 'ACU1000',
    vendor: '翼辉信息',
    status: 'stable',
    diffs: [
      { label: 'PHY 接口',    value: 'rgmii',                  code: true  },
      { label: 'GMAC0 delay', value: 'tx=0x2a / rx=0x19',     code: true  },
      { label: 'GMAC1 delay', value: 'tx=0x4b / rx=0x20',     code: true  },
      { label: 'PHY 复位',    value: 'gpio2 PC1 / gpio4 PB3', code: true  },
    ],
    notes: [
      'delay 配置与 LubanCat-2 相同',
      'PHY 复位时序：延迟 20ms / 20ms / 100ms',
    ],
    snippet:
`&gmac0 {
    tx_delay = <0x2a>;
    rx_delay = <0x19>;
    snps,reset-gpio = <&gpio2 RK_PC1 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <20000 20000 100000>;
};
&gmac1 {
    tx_delay = <0x4b>;
    rx_delay = <0x20>;
    snps,reset-gpio = <&gpio4 RK_PB3 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <20000 20000 100000>;
};`,
  },
  {
    name: 'SND_PLC',
    vendor: '翼辉信息',
    status: 'stable',
    diffs: [
      { label: 'PHY 接口',    value: 'rgmii',                  code: true  },
      { label: 'GMAC0 delay', value: 'tx=0x3c / rx=0x2f',     code: true  },
      { label: 'GMAC1 delay', value: 'tx=0x4f / rx=0x26',     code: true  },
      { label: 'PHY 复位',    value: 'gpio0 PC3 / gpio0 PC4', code: true  },
    ],
    notes: [
      'PHY 复位 GPIO 使用 gpio0（与其他板卡不同），复位时序：0 / 100ms / 200ms',
      'delay 值与 EVB1 相同',
    ],
    snippet:
`&gmac0 {
    snps,reset-gpio = <&gpio0 RK_PC3 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 100000 200000>;
    tx_delay = <0x3c>;
    rx_delay = <0x2f>;
};
&gmac1 {
    snps,reset-gpio = <&gpio0 RK_PC4 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 100000 200000>;
    tx_delay = <0x4f>;
    rx_delay = <0x26>;
};`,
  },
  {
    name: 'ADP（瑞朴智联）',
    vendor: '瑞朴智联',
    status: 'stable',
    diffs: [
      { label: 'PHY 接口',    value: 'rgmii',                  code: true  },
      { label: '时钟方向',    value: 'input（外部时钟）',      code: false },
      { label: 'GMAC0 delay', value: 'tx=0x2d / rx=0x2c',     code: true  },
      { label: 'GMAC1 delay', value: 'tx=0x3a / rx=0x29',     code: true  },
      { label: 'PHY 复位',    value: 'gpio2 PD3 / gpio2 PD1', code: true  },
    ],
    notes: [
      '支持多 slot 配置（slot1~5），各 slot 共用同一 GMAC 配置',
      'GMAC0/1 均额外输出 CLK_MACx_OUT（25 MHz）供 PHY 参考时钟',
    ],
    snippet:
`&gmac0 {
    phy-mode = "rgmii";
    clock_in_out = "input";
    tx_delay = <0x2d>;
    rx_delay = <0x2c>;
    snps,reset-gpio = <&gpio2 RK_PD3 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
    assigned-clock-parents = <...SCLK_GMAC0_RGMII_SPEED>, <&gmac0_clkin>, <...CLK_MAC0_2TOP>;
    assigned-clock-rates = <0>, <125000000>, <25000000>;
};
&gmac1 {
    phy-mode = "rgmii";
    clock_in_out = "input";
    tx_delay = <0x3a>;
    rx_delay = <0x29>;
    snps,reset-gpio = <&gpio2 RK_PD1 GPIO_ACTIVE_LOW>;
    snps,reset-delays-us = <0 20000 100000>;
    assigned-clock-parents = <...SCLK_GMAC1_RGMII_SPEED>, <&gmac1_clkin>, <...CLK_MAC1_2TOP>;
    assigned-clock-rates = <0>, <125000000>, <25000000>;
};`,
  },
]
</script>
<div class="plat-hero">
  <div class="plat-hero-left">
    <div class="plat-breadcrumb"><a href="/products/nic-driver/dw/">DW GMAC</a> / RK3568</div>
    <h1 class="plat-title">RK3568</h1>
    <p class="plat-mfr">Rockchip · ARM Cortex-A55 四核 · 2GHz</p>
    <VersionBadge product="nic-driver/dw" />
    <a class="armory-link" href="http://10.7.1.31/acohub/armory/" target="_blank">Armory 获取</a>
  </div>
  <div class="plat-hero-stats">
    <div class="plat-stat"><span class="ps-val">1G×2</span><span class="ps-label">网口速率</span></div>
    <div class="plat-stat"><span class="ps-val">RGMII</span><span class="ps-label">PHY 接口</span></div>
    <div class="plat-stat"><span class="ps-val">TSO</span><span class="ps-label">硬件卸载</span></div>
    <div class="plat-stat"><span class="ps-val">PTP</span><span class="ps-label">硬件时间戳</span></div>
  </div>
</div>

## 板卡配置

不同板卡的差异主要在时钟方向、TX/RX delay 值和 PHY 复位 GPIO，其余初始化参数一致：

<BoardTabs :boards="boards" />

## SoC 级说明

### 网口映射

| 网口 | 控制器基地址 | IRQ |
|------|------------|-----|
| eth0 (GMAC0) | `0xFE2A0000` | 27 |
| eth1 (GMAC1) | `0xFE010000` | 32 |

### 时钟配置

RK3568 GMAC 支持两种时钟模式：

- **output 模式**（`clock_in_out = "output"`）：时钟由 SoC 内部 PLL（`CLK_MAC_2TOP`）产生并驱动至 PHY，典型值 125 MHz；
- **input 模式**（`clock_in_out = "input"`）：时钟由外部 PHY 或晶振提供，SoC 从 `gmac_clkin` 接收。部分板卡还通过 `CLK_MAC_OUT`（25 MHz）向 PHY 提供参考时钟。

### RGMII 延迟校准

RK3568 通过 GRF 寄存器控制 TX/RX 内部延迟，不同板卡因 PCB 走线不同而使用不同的 delay 值。错误的 delay 配置会导致链路不稳定或无法建立，调试时可逐步调整。

## 已知问题

<div class="issues">
  <div class="issue">
    <span class="issue-tag open">已知</span>
    <span>PHY 自协商在 100M 半双工模式下偶发超时，建议业务侧固定速率为 1000M 全双工</span>
  </div>
</div>

[← 返回 DW GMAC 更新日志](../changelog)

<style>
.plat-hero {
  display: flex; align-items: flex-start; justify-content: space-between;
  flex-wrap: wrap; gap: 1.5rem; padding: 1.5rem; border-radius: 14px;
  border: 1px solid var(--card-border); background: var(--card-bg); margin: 1rem 0 2rem;
}
.plat-breadcrumb { font-size: 0.78rem; color: var(--vp-c-text-3); margin-bottom: 0.5rem; }
.plat-breadcrumb a { color: var(--vp-c-brand-1); text-decoration: none; }
.armory-link { display: inline-block; margin-left: 0.5rem; font-size: 0.68rem; font-weight: 700; padding: 3px 10px; border-radius: 20px; background: rgba(59,130,246,.12); color: var(--vp-c-brand-1); text-decoration: none !important; }
.armory-link:hover { opacity: 0.8; }
.plat-title { font-size: 1.8rem; font-weight: 800; letter-spacing: -0.03em; margin: 0 0 0.25rem; }
.plat-mfr { font-size: 0.85rem; color: var(--vp-c-text-3); margin: 0 0 0.75rem; }
.plat-badge { font-size: 0.68rem; font-weight: 700; padding: 3px 10px; border-radius: 20px; }
.plat-badge.stable { background: rgba(34,197,94,.12); color: #22c55e; }
.plat-badge.beta   { background: rgba(234,179,8,.12);  color: #eab308; }
.plat-hero-stats { display: grid; grid-template-columns: repeat(2,1fr); gap: 0.6rem; }
.plat-stat {
  display: flex; flex-direction: column; align-items: center;
  padding: 0.6rem 1rem; border-radius: 10px;
  background: rgba(59,130,246,.06); border: 1px solid rgba(59,130,246,.1);
}
.ps-val   { font-size: 1rem; font-weight: 800; color: var(--vp-c-brand-1); }
.ps-label { font-size: 0.65rem; color: var(--vp-c-text-3); margin-top: 2px; }
.issues { display: flex; flex-direction: column; gap: 0.5rem; margin: 1rem 0; }
.issue {
  display: flex; align-items: center; gap: 0.6rem;
  padding: 0.6rem 0.9rem; border-radius: 8px;
  border: 1px solid var(--card-border);
  font-size: 0.85rem; color: var(--vp-c-text-2);
}
.issue-tag { font-size: 0.65rem; font-weight: 700; padding: 2px 7px; border-radius: 4px; flex-shrink: 0; }
.issue-tag.resolved { background: rgba(34,197,94,.12); color: #22c55e; }
.issue-tag.open     { background: rgba(234,179,8,.12);  color: #eab308; }
</style>
